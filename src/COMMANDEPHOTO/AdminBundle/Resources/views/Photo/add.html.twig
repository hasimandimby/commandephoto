{% extends 'baseadmin.html.twig' %}
 {% block rigth_content %}
     <h2>Formulaire d'article</h2>
     <div class="well">
         {{ form_start(form, {'attr': {'class': 'form-horizontal','novalidate': 'novalidate'}}) }}

         {# Les erreurs générales du formulaire. #}
         {{ form_errors(form) }}
         <div class="form-group">
             {# Génération du label. #}
             {{ form_label(form.uai, "UAI", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

             {# Affichage des erreurs pour ce champ précis. #}
             {{ form_errors(form.uai) }}

             <div class="col-sm-4">
                 {# Génération de l'input. #}
                 {{ form_widget(form.uai, {'attr': {'class': 'form-control'}}) }}
             </div>
         </div>
         <br/>
         <div class="form-group">
             {# Génération du label. #}
             {{ form_label(form.niveau, "Classe ", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

             {# Affichage des erreurs pour ce champ précis. #}
             {{ form_errors(form.niveau) }}

             <div class="col-sm-4">
                 {# Génération de l'input. #}
                 {{ form_widget(form.niveau, {'attr': {'class': 'form-control'}}) }}
             </div>
         </div>

         <br/>
         <div class="form-group">
             {# Génération du label. #}
             {{ form_label(form.imagefile, "Image à la une", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}

             {# Affichage des erreurs pour ce champ précis. #}
             {{ form_errors(form.imagefile) }}

             <div class="col-sm-4">
                 {# Génération de l'input. #}
                 {{ form_widget(form.imagefile, {'attr': {'class': 'form-control'}}) }}
             </div>
         </div>
         <br />
         {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) }}

         {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
         {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) }}

         {# Génération automatique des champs pas encore écrits.
            Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
            et tous les champs cachés (type « hidden »). #}
         {{ form_rest(form) }}

         {# Fermeture de la balise <form> du formulaire HTML #}
         {{ form_end(form) }}
     </div>

 {% endblock %}